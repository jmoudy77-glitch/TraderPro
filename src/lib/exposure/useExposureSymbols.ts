"use client";

/**
 * Phase 7 seam:
 * - Exposure symbols = held positions + open orders
 * - For now, return [] (no-op) so Phase 6/6.x can wire without refactor.
 */
export function useExposureSymbols(): string[] {
  return [];
}